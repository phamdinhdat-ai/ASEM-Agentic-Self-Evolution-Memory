You are analyzing a multi-turn conversation. Extract ALL atomic facts, events, and pieces of information from the ENTIRE conversation below.

For each atomic fact, output a JSON object with:
- "content": The exact text of the dialogue turn containing this fact (preserve speaker labels)
- "keywords": 3-5 key terms from this fact
- "tags": 1-3 categorical labels (e.g., "personal", "work", "health", "travel", "relationship", "event", "opinion", "plan")
- "description": A 1-2 sentence factual summary of what was learned

Rules:
1. Extract EVERY distinct piece of information — do not merge facts from different turns
2. If a turn contains multiple distinct facts, create separate entries
3. Preserve temporal information (dates, times, sequences)
4. Include speaker attribution in the content
5. Output ONLY a JSON array, nothing else

Conversation:
{dialogue}

Output a JSON array like:
[{{"content": "[Speaker] ...", "keywords": [...], "tags": [...], "description": "..."}}, ...]
Output ONLY the JSON array, nothing else.