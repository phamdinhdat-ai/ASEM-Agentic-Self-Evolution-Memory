

Revise an existing memory note given new related information. Merge keywords/tags, update description. Output JSON:
{{"keywords": [...], "tags": [...], "description": "updated 1-2 sentence summary"}}

Output ONLY the JSON object, nothing else.

Existing note: {existing_note}
New note: {new_note}
